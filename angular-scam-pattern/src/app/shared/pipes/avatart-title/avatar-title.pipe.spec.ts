import { AvatarTitlePipe } from './avatar-title.pipe';

describe('AvatarTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
